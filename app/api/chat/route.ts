import { NextResponse } from "next/server";import{demoChatResponse,retrieveRelevantSiteContent}from"@/lib/chatbot";
const system="You represent LetssAI. Answer only about LetssAI services, industries, business automation, and contacting LetssAI. Avoid unsupported claims. Recommend booking a call for pricing, custom estimates, integration scoping, or business-specific analysis. Do not provide legal, medical, or financial advice.";
export async function POST(req:Request){const body=await req.json().catch(()=>({}));const messages=(body.messages||[]) as {role:string;content:string}[];const last=messages.at(-1)?.content||"";const snippets=await retrieveRelevantSiteContent(last);if(!process.env.OPENAI_API_KEY)return NextResponse.json({message:demoChatResponse(last,snippets)});return NextResponse.json({message:`${system}\n\n${demoChatResponse(last,snippets)}`})}
