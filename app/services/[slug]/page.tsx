import{notFound}from"next/navigation";import{Breadcrumbs}from"@/components/site/Breadcrumbs";import{FAQSection}from"@/components/site/FAQSection";import{CTASection}from"@/components/site/CTASection";import{JsonLd}from"@/components/seo/JsonLd";import{absoluteUrl,breadcrumbsSchema,faqSchema,pageMetadata,serviceSchema}from"@/lib/seo";import{services}from"@/lib/site";
export function generateStaticParams(){return services.map(s=>({slug:s.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const s=services.find(x=>x.slug===slug);return s?pageMetadata(s.seoTitle,s.metaDescription,s.href):{}}
export default async function ServicePage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const s=services.find(x=>x.slug===slug);if(!s)notFound();return <><JsonLd data={serviceSchema(s.title,s.description,s.href)}/><JsonLd data={faqSchema(s.faqs)}/><JsonLd data={breadcrumbsSchema([{name:"Home",url:absoluteUrl("/")},{name:"Services",url:absoluteUrl("/services")},{name:s.title,url:absoluteUrl(s.href)}])}/><section className="section bg-emerald-50"><div className="mx-auto max-w-6xl"><Breadcrumbs items={[{label:"Home",href:"/"},{label:"Services",href:"/services"},{label:s.title,href:s.href}]}/><h1 className="mt-6 text-4xl font-bold md:text-6xl">{s.title} for faster, clearer business communication</h1><p className="mt-5 max-w-3xl text-lg text-slate-600">{s.metaDescription}</p></div></section><Info title="What is this service in simple terms?" text={s.description}/><Grid title="What business problems does it solve?" items={s.problems}/><Grid title="What does the AI assistant do?" items={s.does}/><Grid title="What is an example workflow?" items={s.workflow} ordered/><Grid title="Which channels and tools can it support?" items={s.tools}/><Info title="How does human handoff and safety work?" text="LetssAI is designed to route sensitive, unclear, or high-value conversations to a person. The assistant supports business workflows and does not replace professional legal, medical, or financial judgment."/><Grid title="What business benefits can you expect?" items={s.benefits}/><Grid title="Which industries use this service?" items={s.industries}/><FAQSection faqs={s.faqs}/><CTASection title={`Want to explore ${s.title}?`}/></>}
function Info({title,text}:{title:string;text:string}){return <section className="section"><div className="mx-auto max-w-4xl"><h2 className="text-3xl font-bold">{title}</h2><p className="mt-4 text-lg text-slate-600">{text}</p></div></section>}
function Grid({title,items,ordered}:{title:string;items:string[];ordered?:boolean}){const Tag=ordered?"ol":"ul";return <section className="section bg-slate-50/70"><div className="mx-auto max-w-6xl"><h2 className="text-3xl font-bold">{title}</h2><Tag className="mt-8 grid gap-4 md:grid-cols-2">{items.map((x,i)=><li className="rounded-3xl bg-white p-6 text-slate-700 shadow-sm" key={x}>{ordered&&<b className="mr-2 text-emerald-700">{i+1}.</b>}{x}</li>)}</Tag></div></section>}
