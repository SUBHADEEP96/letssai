import type { ComponentType } from "react"
import type { IconProps } from "@phosphor-icons/react"
import { CalendarDots, ChatCircleText, CheckCircle, FileText, Funnel, Gauge } from "@phosphor-icons/react/dist/ssr"
import type { GalleryItem } from "@/lib/content/types"
const icons: Record<GalleryItem["visualType"], ComponentType<IconProps>> = { pipeline: Funnel, chat: ChatCircleText, calendar: CalendarDots, timeline: CheckCircle, checklist: CheckCircle, documents: FileText, dashboard: Gauge }
export function ProductMockupCard({ item }: { item: GalleryItem }) { const Icon = icons[item.visualType]; return <article className="group overflow-hidden rounded-[1.75rem] border border-emerald-950/10 bg-white shadow-lg shadow-emerald-950/5"><div className="min-h-52 bg-gradient-to-br from-emerald-950 to-emerald-800 p-5"><div className="rounded-2xl border border-white/15 bg-white/10 p-4 backdrop-blur"><div className="flex items-center justify-between text-emerald-50"><Icon size={22} aria-hidden /><span className="text-[10px] tracking-widest uppercase">Live preview</span></div><div className="mt-5 grid gap-2">{item.labels.map((label, i) => <div className="flex items-center gap-3 rounded-xl bg-white p-3 text-xs font-medium text-slate-800 shadow-sm" key={label}><span className={`size-2 rounded-full ${i === item.labels.length - 1 ? "bg-emerald-500" : "bg-emerald-200"}`} />{label}<span className="ml-auto h-1.5 rounded-full bg-emerald-100" style={{ width: `${30 + i * 12}%` }} /></div>)}</div></div></div><div className="p-6"><h3 className="text-xl font-semibold tracking-tight">{item.title}</h3><p className="mt-2 text-sm leading-6 text-slate-600">{item.description}</p></div></article> }
