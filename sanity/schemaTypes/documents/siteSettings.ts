import { defineField, defineType } from "sanity"
export const siteSettings = defineType({ name: "siteSettings", title: "Site Settings", type: "document", fields: [defineField({ name: "title", type: "string", validation: (r) => r.required() }), defineField({ name: "description", type: "text" }), defineField({ name: "contactEmail", type: "email" }), defineField({ name: "primaryCta", type: "ctaObject" }), defineField({ name: "homepageSections", type: "array", of: [{ type: "object", fields: [defineField({ name: "heading", type: "string" }), defineField({ name: "answer", type: "text" }), defineField({ name: "items", type: "array", of: [{ type: "string" }] })] }] }), defineField({ name: "seo", type: "seoFields" })] })
